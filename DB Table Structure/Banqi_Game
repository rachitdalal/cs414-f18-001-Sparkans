CREATE TABLE sparkans.Banqi_Game (
	game_id INT NOT NULL  AUTO_INCREMENT,
    user1 VARCHAR(50) NOT NULL UNIQUE,
    user2 VARCHAR(50) NOT NULL UNIQUE,
    current_state BLOB,
    game_status VARCHAR(50),
    PRIMARY KEY (game_id)
);
