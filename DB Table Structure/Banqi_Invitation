CREATE TABLE sparkans.Banqi_Invitation (
	invitation_id INT NOT NULL  AUTO_INCREMENT,
    sent_user VARCHAR(50) NOT NULL,
    received_user VARCHAR(50) NOT NULL,
    status VARCHAR(50) DEFAULT 'Waiting',
    PRIMARY KEY (invitation_id)
);
